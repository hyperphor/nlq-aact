# Need the UI to be more webbish

## Query on front page

That is, home page should have the query UI front and center.

Maybe get rid of the tabs or make them look more like a web app. (eg, instead of literal tabs, have some links at the top)

The About and Schema stuff should still be there.

I'm tempted to say the data viz stuff should have its own page (aka tab).

Yes OK, layout should be
- nav
- big obvious query box (with examples). Bigger font.
- below that, the data grid and side-cards.

Retain the hyperphor-styled top bar

